public class Arr
{
    public void arrint(integer n)
    {
        List<Integer> listarr = new List<Integer>();
           
        for(integer i=0;i<n;i++)
        {
            system.debug(listarr[i]);
        }
    }
}